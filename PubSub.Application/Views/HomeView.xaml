<UserControl
    x:Class="PubSub.Application.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:PubSub.Application.ViewModels"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <d:UserControl.DataContext>
        <viewModels:HomeViewModel />
    </d:UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary Source="../ResourceDictionary.xaml" />
    </UserControl.Resources>
    <DockPanel>
        <DockPanel DockPanel.Dock="Top">
            <TextBlock Text="Base Url" />
            <TextBox Text="{Binding BaseUrl}" />
        </DockPanel>
        <UniformGrid Columns="2">
            <ContentControl Content="{Binding SubscriberViewModel}" />
            <ContentControl Content="{Binding PublisherViewModel}" />
        </UniformGrid>
    </DockPanel>
</UserControl>
