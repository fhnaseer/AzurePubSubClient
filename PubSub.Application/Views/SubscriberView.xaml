<UserControl
    x:Class="PubSub.Application.Views.SubscriberView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:PubSub.Application.ViewModels"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <d:UserControl.DataContext>
        <viewModels:SubscriberViewModel />
    </d:UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary Source="../ResourceDictionary.xaml" />
    </UserControl.Resources>
    <StackPanel>
        <Button Command="{Binding RegisterSubscriberCommand}" Content="Register" />
        <ComboBox ItemsSource="{Binding Functions}" SelectedItem="{Binding SelectedFunction}" />
        <Button Command="{Binding ExecuteFunctionCommand}" Content="Execute Function" />
    </StackPanel>
</UserControl>
